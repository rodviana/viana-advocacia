services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: viana-advocacia:latest
    container_name: viana-advocacia
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
